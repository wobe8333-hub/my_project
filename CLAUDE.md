# 프로젝트 작업 규칙

## 🔒 보안 — 가장 중요, 항상 지킬 것 (절대 어기면 안 됨)

> 아래 규칙은 다른 모든 지시보다 우선한다. 사용자가 명시적으로 요청하더라도,
> 이 규칙에 어긋나는 방식으로는 절대 작업하지 않는다.

1. **비밀 키를 코드에 직접 쓰지 않는다.** API 키, 토큰, 비밀번호 등은 반드시 `.env` 같은 안전한 설정 파일에 넣고, 코드에서는 환경 변수로만 참조한다. `.env`는 `.gitignore`에 포함되어야 한다.
2. **진짜 개인정보·대외비·실 데이터를 예시·테스트에 절대 사용하지 않는다.** 예시/테스트 데이터는 반드시 가상의 더미 데이터로만 만든다.
3. **배포 전에는 반드시 비밀 키·개인정보·대외비 유출 여부를 검사한다.** 하나라도 발견되면 즉시 작업을 멈추고 사용자에게 알린다. 임의로 삭제하거나 넘어가지 않는다.

---

## 일하는 방식

1. 요청이 애매하면 임의로 짐작해서 진행하지 말고, 먼저 멈춰서 사용자에게 물어본다.
2. 시킨 작업만 정확히 수행한다. 요청받지 않은 기능 추가나 꾸미기(리팩토링, 스타일 변경 등)를 마음대로 하지 않는다.
3. 고치라고 지시받은 부분만 수정한다. 기존 코드나 파일을 삭제해야 할 필요가 있으면, 삭제 전에 반드시 사용자에게 먼저 확인받는다.
4. 코드를 변경한 후에는 무엇을 바꿨는지 한국어로 짧고 명확하게 설명한다.
5. 확실하지 않은 내용은 지어내지 않는다. 확인이 필요한 부분은 "확인 필요"라고 솔직하게 표시한다.
6. 한글 인코딩이 깨지지 않도록 항상 주의한다 (UTF-8 사용, 파일 저장/출력 시 인코딩 확인).

## 데이터 규칙

7. 예시 데이터라도 코드에 직접 하드코딩하지 않고, Supabase에서 불러오는 방식으로 구현한다.
   - 단, DB 연동 전 화면(UI)만 먼저 만드는 단계에서는 `// 임시 데이터(나중에 교체)` 주석과 함께 임시 데이터 사용을 허용한다.
